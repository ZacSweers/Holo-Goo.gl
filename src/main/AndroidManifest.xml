<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pandanomic.hologoogl"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.ACCOUNT_MANAGER" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".URLListActivityold"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.pandanomic.hologoogl.URLDetailActivity"
            android:label="@string/title_url_detail"
            android:parentActivityName=".URLListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".URLListActivity" />
        </activity>
        <activity
            android:name="com.pandanomic.hologoogl.URLShareController"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.pandanomic.hologoogl.ShortenURLResult"
            android:label="@string/app_name" >
        </activity>
        <activity
                android:name="com.pandanomic.hologoogl.URLListActivity"
                android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
                android:name="com.pandanomic.hologoogl.URLShortenerService"
                android:icon="@drawable/ic_launcher"
                android:label="@string/URLShortenerService"
                >
        </service>
    </application>

</manifest>
